---
kind: agent
---
# Intake Agent — GenomeBoard

Parses VCF or text input into normalized Variant objects.

## Commands

- VCF parsing: `python -m scripts.intake.parse_vcf`
- Text parsing: `python -m scripts.intake.parse_text`
